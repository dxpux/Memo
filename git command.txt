如果你裝了 Git for Windows，可以不用特別去設置 git 的環境變數，
右鍵即可點選 "Git Bash Here" 一般的 git 指令，在這已夠用。

當然，安裝 TortoiseGit 也很方便

GitHub 支援線上編輯
線上編輯時，曾遇到編碼問題
We've detected the file encoding as Big5. When you commit changes we will transcode it to UTF-8.

Amend last commint 修改最後一次更新。
效果相當於 Reset Mixed 到上一版, 再 Commit. (Mixed 會在 master  node 移動時，並不同步修改工作目錄)